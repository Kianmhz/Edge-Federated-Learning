# ============================================================
# FEDERATED LEARNING PROJECT - PYTHON DEPENDENCIES
# ============================================================
# Install with: pip install -r requirements.txt
#
# BREAKDOWN BY PURPOSE:
#
# 1. WEB FRAMEWORK (Server/API)
# 2. MACHINE LEARNING (Model Training)
# 3. DATA PROCESSING (Dataset Handling)
# 4. HTTP CLIENT (Client-Server Communication)
# ============================================================

# ============================================================
# 1. WEB FRAMEWORK - FastAPI Stack
# ============================================================

# FastAPI - Modern, fast web framework for building APIs
# WHY: Creates REST API endpoints for client-server communication
# EXAMPLE: @app.get("/get_model") creates an endpoint
fastapi==0.109.2

# Uvicorn - ASGI server to run FastAPI
# WHY: FastAPI needs a server to actually run and accept requests
# ANALOGY: FastAPI is a restaurant, Uvicorn is the building it runs in
uvicorn[standard]==0.27.1

# Pydantic - Data validation using Python type hints
# WHY: Validates incoming data automatically (comes with FastAPI)
# EXAMPLE: UpdateRequest(BaseModel) validates client submissions
pydantic==2.5.3

# ============================================================
# 2. MACHINE LEARNING - PyTorch Stack
# ============================================================

# PyTorch - Deep learning framework
# WHY: Used for defining, training, and evaluating neural networks
# NOTE: This installs CPU version. For GPU: see https://pytorch.org/get-started/locally/
# Updated to work with Python 3.12+
torch==2.5.0

# TorchVision - Computer vision datasets and transforms
# WHY: Provides MNIST dataset and image preprocessing
# EXAMPLE: datasets.MNIST() loads the handwritten digits dataset
# Updated to match torch version
torchvision==0.20.0

# ============================================================
# 3. DATA PROCESSING
# ============================================================

# NumPy - Numerical computing library
# WHY: Array operations, data splitting, Dirichlet distribution
# EXAMPLE: np.random.dirichlet() for non-IID data partitioning
# NOTE: 1.x does NOT support Python 3.12 → updated to 2.0+
numpy==2.0.1

# ============================================================
# 4. HTTP CLIENT - For Client-Server Communication
# ============================================================

# Requests - HTTP library for making API calls
# WHY: Clients use this to communicate with server
# EXAMPLE: requests.post(f"{SERVER_URL}/submit_update", json=data)
requests==2.31.0

# ============================================================
# 5. AZURE SERVICE BUS - Event-Driven Messaging
# ============================================================

# Azure Service Bus - Message broker for FL pipeline communication
# WHY: Replaces HTTP polling with event-driven pub/sub (topics + queues)
# USAGE: Topics for round-control & model broadcast, queues for updates
azure-servicebus>=7.12.0

# Azure Blob Storage - Claim-check pattern for large payloads
# WHY: Model weights (~5 MB) exceed Service Bus message limits; store in
#       blob storage and pass a lightweight reference through the bus
azure-storage-blob>=12.19.0

# Azure Identity - Authentication support
# WHY: Enables DefaultAzureCredential for managed identity / local dev
azure-identity>=1.15.0
